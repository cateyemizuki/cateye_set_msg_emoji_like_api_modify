# 🐷！表情回应插件 — 工具与触发词说明

## 指令

本插件**不注册任何指令**（调试命令已移除），所有能力通过 LLM 工具与 Hook 自动触发。

## LLM 工具（自然语言触发）

| 工具 | 触发场景示例 | 参数 |
|------|-------------|------|
| `emoji_like` | 模型想表达赞同/开心/无奈/可爱等情绪，或想与群友互动时（如「给这条消息点个赞」「贴个表情」） | `emoji`（可选：描述库中的表达如「猪猪表情」，或直接表情 ID；留空用默认点赞）、`target_message_id`（可选：目标消息 ID；留空自动定位最近一条非机器人消息） |

> 工具描述引导：默认贴到最近一条非机器人消息（通常正在回复/讨论的那条）；调用后模型应在回复中自然提及这次贴表情，避免重复表达。

## 表情回应通知翻译（Hook 自动触发）

群友对消息贴表情时，NapCat 会推送 `group_msg_emoji_like` 通知，插件自动翻译为可读文本并注入框架：

| 场景 | 输出示例 |
|------|---------|
| 仅对照表命中 | `[事件-群消息表情回应] 凯特艾 对消息(ID:-1683989482)表达了 [玫瑰]` |
| 对照表 + 描述库命中 | `[事件-群消息表情回应] 凯特艾 对消息(ID:-1683989482)表达了 玫瑰：<描述库中的具体含义>` |
| 未知表情（默认） | `[事件-群消息表情回应] 凯特艾 对消息(ID:-1683989482)表达了 [未知表情999999]` |
| 未知表情（开启优化） | `[事件-群消息表情回应] 凯特艾 对消息(ID:-1683989482)表达了 一个表情` |

## 群友是🐷（自动贴表情，默认关闭）

无需指令，开启 `[pig_friends] enabled = true` 后自动生效：

- **普通用户**：发消息 → 按 `normal_probability` 概率命中 → 贴 12951 → 全局冷却 `normal_cooldown_seconds`；
- **猪友（`pig_users`）**：发消息 → 冷却过即贴（独立冷却、不看概率与黑白名单）→ 贴后按 `pig_chain_skip_cooldown_probability` 概率免冷却连贴下一条消息，最多 `pig_max_chain` 条。
